export class ArchitectService {
  generate(data: any): any {
    return { blueprint: {} };
  }

  getBlueprint(id: string): any {
    return { id };
  }

  listBlueprints(): any[] {
    return [];
  }
}
